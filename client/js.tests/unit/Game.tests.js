/// <reference path="../qunit/qunit.js" />
/// <reference path="../../../model/Model.js" />

/**********************************************************************/
module('Game.js');
/**********************************************************************/
/*
test('ctor_Game', function () {
  //arrange

  //act
  var game = new Game();

  //assert
  ok(game.map instanceof Map, 'a new Game must contains a Map');
  equals(typeof game.map.controller, 'undefined', 'a new Game must map\' controller is not defined');
  equals(game.map.tiles.length, 0, 'a new Game\'s map must be empty');
});

test('ctor_MapController_setsMapController', function () {
  //arrange
  var controller = new MapController();

  //act
  var game = new Game(controller);

  //assert
  ok(game.map.controller === controller, 'game\'s map ctroller must be instance passed to the Game\'s constructor');
});
*/
